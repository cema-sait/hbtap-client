services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-https://hta.cema.africa/api}

    image: hbtap-client
    container_name: hbtap-client
    restart: unless-stopped

    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL:-https://hta.cema.africa/api}

    networks:
      - proxy

    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"

      - "traefik.http.routers.bptap.rule=Host(`hta.cema.africa`)"
      - "traefik.http.routers.bptap.entrypoints=websecure"

      - "traefik.http.routers.bptap.tls=true"
      - "traefik.http.routers.bptap.tls.certresolver=letsencrypt"

      - "traefik.http.services.bptap.loadbalancer.server.port=3000"

networks:
  proxy:
    external: true
