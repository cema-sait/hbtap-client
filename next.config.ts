/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
   unoptimized: true,
  },
  // experimental: {
  //   turbopack: {
  //     resolveAlias: {},
  //   },
  // },
};

export default nextConfig;

